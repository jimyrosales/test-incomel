@page
@model jimyTest.Pages.Employees.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Employee[0].DPI)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Employee[0].childrenNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Employee[0].baseSalary)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Employee[0].decreeBond)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Employee[0].Active)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Employee) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DPI)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.childrenNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.baseSalary)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.decreeBond)
            </td>
            <!--td>
                @Html.DisplayFor(modelItem => item.Active)
            </td-->
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
