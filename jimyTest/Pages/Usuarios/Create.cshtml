@page
@model jimyTest.Pages.Usuarios.CreateModel

@{
    ViewData["Title"] = "Register";
}

<h1>Register</h1>

<h4>Usuario</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Usuario.Name" class="control-label"></label>
                <input asp-for="Usuario.Name" class="form-control" />
                <span asp-validation-for="Usuario.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Usuario.BirthDate" class="control-label"></label>
                <input type="date" asp-for="Usuario.BirthDate" class="form-control" />
                <span asp-validation-for="Usuario.BirthDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Usuario.Password" class="control-label"></label>
                <input type="password" asp-for="Usuario.Password" class="form-control" id="password" />
                <span asp-validation-for="Usuario.Password" class="text-danger"></span>
            </div>
            <!--div class="form-group">
                <label class="control-label">Confirm</label>
                <input type="password" class="form-control" id="confirmPassword" onkeypress="verifyPassword(this);" />
                <span id="passwordInformation"></span>
            </!--div>
            <div class="form-group">
        <label asp-for="Usuario.RecoveryPassword" class="control-label"></label>
        <input asp-for="Usuario.RecoveryPassword" class="form-control" />
        <span asp-validation-for="Usuario.RecoveryPassword" class="text-danger"></span>
    </div>
    <div class="form-group">
        <label asp-for="Usuario.Active" class="control-label"></label>
        <input asp-for="Usuario.Active" class="form-control" />
        <span asp-validation-for="Usuario.Active" class="text-danger"></span>
    </div-->
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

<!--script type="text/javascript">
    function verifyPassword(repeatPassword.target) {

        var textSpanErr = "La contraseña no coincide.";
        var textSpanInf = "La contraseña coincide.";

        var passwordSpan = document.getElementById("passwordInformation");
        var password = document.getElementById("password");

        if (repeatPassword.text !== password.text) {
            passwordSpan.attributes["color"] = "red";
            passwordSpan.innerText = textSpanErr;
        }
    }
</script-->

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
