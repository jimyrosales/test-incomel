@page
@model jimyTest.Pages.Usuarios.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Usuario</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Usuario.Id" />
            <div class="form-group">
                <label asp-for="Usuario.Name" class="control-label"></label>
                <input asp-for="Usuario.Name" class="form-control" />
                <span asp-validation-for="Usuario.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Usuario.BirthDate" class="control-label"></label>
                <input asp-for="Usuario.BirthDate" class="form-control" />
                <span asp-validation-for="Usuario.BirthDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Usuario.Password" class="control-label"></label>
                <input asp-for="Usuario.Password" class="form-control" />
                <span asp-validation-for="Usuario.Password" class="text-danger"></span>
            </div>
            
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
